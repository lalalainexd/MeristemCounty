<h1>New page</h1>

<% form_for([@comic, @chapter, @page], :url => comic_chapter_pages_path) do |f| %>
  <%= f.error_messages %>
  <p>
  
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', comic_chapter_path(@comic, @chapter)%>
